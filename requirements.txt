# MarlishAI Python Dependencies
# Install: pip install -r requirements.txt
#
# NOTE: For CUDA GPU support (RTX 3060 etc.), install torch separately FIRST:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
# Then run: pip install -r requirements.txt
#
# For CPU-only: just run pip install -r requirements.txt directly.

transformers>=4.40.0
sentencepiece>=0.2.0
sacrebleu>=2.4.0
indic-transliteration>=2.3.0
pandas>=2.2.0
pyarrow>=15.0.0
