# MarlishAI API — Production Dependencies
# For Hugging Face Spaces deployment (Free tier — CPU)

# PyTorch CPU-only (smaller image, no CUDA needed on free tier)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0

# NLP / Translation
transformers>=4.40.0,<5.0.0
sentencepiece>=0.2.0

# API server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Gemini GEC post-processing (free tier: 15 RPM, 1M tokens/day)
google-genai>=1.0.0

# Transliteration deps
indic-transliteration>=2.3.0
